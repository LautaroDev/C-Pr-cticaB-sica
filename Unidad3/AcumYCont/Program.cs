/* int contador = 0, e1 = 20, e2 = 30, e3 = 15,promedio; //Lo normal es que el contador se inicie en 0
int acumulador = 0;
if (e1 > 18){
    contador = contador + 1;
    acumulador = acumulador + e1;   
}
    //Esto se puede simplificar con un contador de incremento;
//Elcontador de incremento se usa como ++ o --, se pueden usar con cualquier variable numerica 
if (e2 > 18){
    contador++;
    acumulador = acumulador + e2;
}

if (e3 > 18)
{ //como hay mas de una instrucción va el corchete
    contador++;
    acumulador = acumulador + e3;
}
promedio = acumulador / contador;
Console.WriteLine("El contador conto: " + contador +  " Y acumulo: " + acumulador +  " El promedio es: " + promedio);

 */


